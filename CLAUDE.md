# CLAUDE.md｜ライター副業ラボ プロジェクト憲法

---

## 1. このブログの目的

副業を挫折した20代会社員が「自分にもWebライターはできるか」を確かめに来る場所。
読者の出口：クラウドワークスで初案件を取り、月1〜3万円を稼げる状態になること。

- **URL**: https://tsuzuku-lab.mods.jp/wordpress/
- **プラットフォーム**: WordPress（テーマ: Cocoon）

---

## 2. ペルソナ

### 2-1 主要ペルソナ

24〜28歳・男性・会社員・手取り20万前後。副業を1〜3回挫折済み。
本音：【恐怖】また失敗するかも／【焦り】このままじゃヤバい／【疑問】スキルなしでも本当にできるのか

**詳細 → `assets/02_persona.md`**

### 2-2 複数属性混在パターン（重要）

KW によっては、検索者の属性が混在する。記事設計時に必ず分析する：

| KWパターン例 | 想定される属性 |
|---|---|
| 「〇〇 やめとけ」「〇〇 向いてない」 | (A)始める前の検討者 ＋ (B)始めて伸び悩んでる人 |
| 「〇〇 稼げない」 | (A)稼げず迷ってる人 ＋ (B)これから始める人 |
| 「〇〇 やり方」 | (A)初心者 ＋ (B)経験者で再学習したい人 |

→ **属性が混在する場合、CTA設計も属性別に分岐する**（詳細は §5-4）。

---

## 3. 絶対禁止事項

1. **NG表現使用禁止**: 執筆・校閲時は必ず `assets/01_ng-words.md` を参照
2. **体験捏造禁止**: `me.md` に記録がない数値・エピソードは書かない。なければ【要一次情報】と書いて止まる
3. **KW独断変更禁止**: メインKWは人間が選定。Claude Code が独自変更することは禁止。KWがなければ確認を求める
4. **文体・口語の自動生成禁止**: 文体・一次情報・口語表現はユーザー100%担当。Claude Code が補完・捏造しない
5. **対話ルール必須**: ファイル受け取り時は §8 の対話ルールを必ず適用してから保存
6. **長文引用禁止**: 競合記事からの引用は15文字以内・1記事1ソースにつき1引用まで（著作権遵守）
7. **担当範囲の遵守**:
   - ✅ Claude Code が担当：構造設計・CTA設計・PREP/PASONA等のライティング技法・リサーチ・統計データ
   - ✅ ユーザー100%が担当：文体・一次情報・口語表現
   - ❌ 絶対NG：競合記事の文・段落をそのまま使用（センテンスコピー）

---

## 4. 必須品質基準（全コマンド共通）

### 4-1 構造設計の5原則（/outline で必ず順守）

**原則1：読者感情曲線の一直線性**
H2の並びは、読者感情の自然な遷移（不安→診断→判断→解決→行動）に沿っているか？
→ 視点ジャンプのあるH2配列は再設計する。

**原則2：検索意図の3つの悩み全カバー**
KWで検索する読者の「本能的な悩み」を3つに分解し、全てに答えるH2が用意されているか？
→ 特に「明日から何すれば？」への答えが具体的か？

**原則3：対比構造の検討**
自己診断・選択系のテーマでは、対比軸を2セット用意しているか？
→ 例：「向いてない vs 向いてる」「続ける vs やめる」
→ 一方向のみの列挙は読者の自己認識を妨げる。

**原則4：差別化の方向**
差別化は「著者の個性」ではなく「読者の盲点」を埋める方向か？
→ 著者の体験談だけの差別化は弱い。競合9社が言ってない読者ニーズに応えるか？

**原則5：CTA属性分岐**
検索意図に複数属性が含まれる場合、CTAも属性別に分岐しているか？
→ 1属性のみのCTAは読者の半数を取り逃がす。

### 4-2 装飾ルール（/write・/polish で必ず順守）

**色使い**：
- 本文中の装飾は **3色のみ**：黒太字 / 黄色マーカー / 赤文字
- 黄色マーカーは **記事全体で1箇所のみ**（最重要メッセージに使用）
- 赤文字は **数字強調のみ**

**装飾密度**：
- 1段落あたりの装飾は **1〜2箇所まで**（ボックス内除く）
- スキャン読みでも要点が伝わるか必ず確認

**ボックスの色分け**（Cocoonタブ見出しボックス/アイコンボックス）：
- 🔴 赤：NG例
- 🔵 青：OK例・予防策
- 🟡 黄：コツ・ヒント・事後対応
- 🟢 緑：補足・CTA

**コードブロック**：
- 用途は「コピペ用テンプレ」のみ（提案文・プロフィール例など）

### 4-3 ファクトチェック・出典ルール（/check で必ず確認）

**数値・統計の扱い**：
- すべての数値・統計には出典が必要
- 出典は本文内にリンクで埋め込む（脚注より本文内リンクを優先）
- 出典がない数値は使用しない（「9割」「過半数」等の曖昧表現も同様）

**著者体験の扱い**：
- 著者の体験談は `me.md` の記録がある内容のみ
- 数値（月収・継続月数・案件数等）は `me.md` 参照必須
- 記録にない場合は【要一次情報】で止まる

**引用の扱い**：
- 1ソースあたり15文字以内
- 1記事内で1ソースから1引用まで
- 「" "」で囲み、出典明示
- パラフレーズ（言い換え）を基本とし、引用は例外的に

---

## 5. 構造設計テンプレ

### 5-1 リード文テンプレ

構造：共感 → 悩みへの共感深掘り → 権威性 → 読者メリット → 結論 → CTA①

```
[共感]
読者の悩み・状況を箇条書き or 文章で言語化。
※検索意図の3つの悩みに対応させると刺さりやすい。

[悩みへの共感深掘り]
箇条書き or 文章で出した悩みに、さらに寄り添う1〜2文。
「分かります、僕も〜」のような peer voice で。

[権威性]
著者が語る資格（体験・実績・数値）を1〜2文で。
※ me.md 参照必須。

[読者メリット]
読み終えた後の読者の状態を1文で。
「この記事を読めば〜になります」形式。

[結論]
KWの問いに対する結論を1〜2文で提示。
※共感→権威性→メリットで信頼を作った後に提示することで、
　断定的な結論でも読者が受け入れやすくなる。

[CTA①]
本文への誘導（「読み進めてください」等の軽い誘導）。
```

文字数目安：300〜400字

### 5-2 まとめテンプレ

構造：本文まとめ → （補足） → CTA

```
[本文まとめ]
要点を箇条書きで4つ前後。
※箇条書きはまとめセクションでこの1箇所のみ。

[補足（あれば）]
本文で扱えない補足が3つ以上ある場合のみ、プロース1段落で記述。
※箇条書きNG。本文と重複する内容は入れない。なければ省略。

[CTA]
属性分岐の場合は2つのCTAボックス、単一属性なら1つ。
著者のpeer messageとマイクロCV（ブックマーク誘導等）で締める。
```

文字数目安：400〜600字

**重要**：H3（見出し3）使用禁止。太字リードによる「H3風セクション化」も禁止。
プロース＋箇条書き＋CTAボックスのみで構成する。

### 5-3 タイトル・スラッグ・メタ生成ルール

**タイトル**：
- 文字数：**28〜32字**（モバイル切れ防止）
- メインKWを **前半30%以内** に配置
- 数字を1つ以上含める（「7選」「3つ」等）
- ベネフィットを明示（「対処法」「判断基準」等）

**スラッグ**：
- **英語のみ、ハイフン区切り、5単語以内**
- 日本語スラッグは使わない（%エンコードで崩れるため）
- KWを直訳ベースに（例：`webwriter-not-suited`）

**メタディスクリプション**：
- 文字数：**110〜120字**（モバイル切れ防止）
- メインKWを前半に含める
- 「悩みへの共感 → 約束する価値 → 信頼性」の順で構成
- peer voice（著者の立場を1文入れる）

### 5-4 属性分岐CTAテンプレ

**単一属性の場合**：
```
🟢 [CTAタイトル]
[サービス概要・メリット3つ]
👉 [公式リンク]
```

**複数属性の場合**（検索者がA/B混在時）：
```
[全員への一言] 共通メッセージ（両属性に効くアドバイス）。

[属性A向けアドバイス] 属性A特有の動き方（プロース1〜2文）。
🟢 [属性A向け CTA]
[サービス概要]
👉 [リンク]

[属性B向けアドバイス] 属性B特有の動き方（プロース1〜2文）。
🟢 [属性B向け CTA]
[サービス概要]
👉 [リンク]
```

**配置原則**：
- CTAボックスは、属性別アドバイスの **直後** に配置
- 「全員向けメッセージ」直後にCTAを置くと、片方の属性に違和感が出る

---

## 6. ワークフロー

```
/research [KW]  → research.md 生成（競合分析・差別化・検索意図）
      ↓
/outline        → H2/H3構成 + 構成の意図 + 一次情報ヒアリング → outline.md
      ↓
ユーザー: 一次情報を回答
      ↓
/write          → PREP法/PASONA法で骨格生成 → body.md
      ↓
ユーザー: 肉付け（声・体験・感情を追記）
      ↓
/check          → 校閲レポート（NG・捏造・5原則チェック）
      ↓
/polish         → メタ・FAQ・タイトル・スラッグ・サムネプロンプト
      ↓
/publish        → WordPress入稿準備
```

| コマンド | 定義状態 | 備考 |
|---------|---------|------|
| `/research` | ✅ 定義済み | §4・§5との整合確認推奨 |
| `/outline` | ✅ 定義済み | §4-1（5原則）組み込み済み |
| `/write` | ✅ 定義済み | §4-2（装飾ルール）組み込み改修推奨 |
| `/check` | ✅ 定義済み | §4（品質基準）組み込み改修推奨 |
| `/polish` | ✅ 定義済み | §5-3（スラッグ・字数）組み込み改修推奨 |
| `/publish` | ✅ 定義済み | 削除済みファイル参照の修正推奨 |

---

## 7. 参照ファイルマップ

| ファイル | 役割 | 使うコマンド |
|---------|------|------------|
| `assets/01_ng-words.md` | NG表現リスト（最優先） | 全コマンド |
| `assets/02_persona.md` | 読者の感情・検索行動・読後状態 | /research・/outline |
| `assets/tone-sample.md` | 著者の声・リズム（参照のみ・模倣禁止） | /write |
| `assets/08_my-templates.md` | SEOライティング絶対基準（§0〜§8） | /outline・/write |
| `assets/07_style-references.md` | 競合記事の構成パターン（構造参考のみ） | /research・/outline |
| `me.md` | 著者の体験・数値・一次情報の唯一の源泉 | /write |

### 衝突時の優先順位
`01_ng-words.md` ＞ `02_persona.md` ＞ `08_my-templates.md` ＞ `07_style-references.md` ＞ `me.md`

| 重要度 | 判定基準 | 対応 |
|---|---|---|
| **高** | 絶対禁止事項（§3）に関わる／数値・出典に関わる／ペルソナ齟齬 | 必ず止まりユーザーに確認 |
| **中** | 文体・トーン・装飾の齟齬 | ユーザーに通知・上位ファイル優先 |
| **低** | 表記揺れ・微細な構成差 | 上位ファイルを優先して続行 |

---

## 8. 対話ルール

### 8-1 ファイル取り込み時（保存前に必ず実行）

1. ファイル内容を要約してユーザーに提示（3〜5行）
2. 既存ファイルとの整合性チェック
3. 矛盾・不明点・曖昧な表現をリストアップ
4. ユーザーに具体的に質問
5. ユーザーの「OK」を得てから保存

### 8-2 コマンド実行時の対話ルール

**/outline 実行時**：
- H2/H3構成案を提示
- §4-1の5原則チェック結果を併記
- 構成意図（各H2 × ペルソナの感情 × 起こす変化の表）を提示
- 【要一次情報】の質問リストを提示
- → ユーザー回答待ち（STOP）
- 回答受取後、outline.md に保存

**/write 実行時**：
- リード文（§5-1）・各H3・まとめ（§5-2）・CTA（§5-4）を §5テンプレに従って生成
- 【要一次情報】の箇所は **生成せず止まる**（捏造禁止）
- 装飾ルール（§4-2）を順守
- セルフチェック（10項目）を実行
- → 出力後、body.md に保存

**/check 実行時**：
- §4-1（5原則）、§4-2（装飾）、§4-3（出典）、§3（禁止事項）を全て検証
- 違反箇所をリストアップ・修正案を提示
- → ユーザー修正後、次のステップへ

**/polish 実行時**：
- §5-3に従ってタイトル・スラッグ・メタを3案ずつ生成
- FAQ要否を判定（本文で扱えない不安が3つ以上あるか）
- サムネ画像プロンプトを生成
- → 全て提示してユーザー承認待ち

**/publish 実行時**：
- WordPress入稿用最終チェックリストを提示
- 内部リンク・アイキャッチ・カテゴリ・タグを提案
- → 承認後、入稿準備完了通知
